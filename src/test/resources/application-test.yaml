spring:
  flyway:
    schemas: test
    create-schemas: true
    url: jdbc:h2:mem:mydb;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
    user: sa
    password: password
  datasource:
    url: jdbc:h2:mem:mydb;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password: password